# -----------------------------
# Terraform local files (NEVER commit)
# -----------------------------
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.*
crash.log
crash.*.log

# Terraform plans (binary + JSON)
*.tfplan
*.tfplan.*
tfplan.json
tfplan.binary

# Evidence bundles
evidence-*.tar.gz
*.sig
*.sha256

# OPA / Conftest
conftest-results.json
*.rego.tmp

# Python virtual envs (if used)
venv/
.env/

# OS / Editor junk
.DS_Store
Thumbs.db
*.swp
*.swo

# Node / build junk (if any future UI)
node_modules/
dist/

# GRC evidence artifacts (never commit)
evidence/